
================================================================================
                          ANALISI UML vs CODICE IMPLEMENTATO
                          Aggiornata al 2026-09-09
================================================================================

Segno convenzione:  ✓ = corrisponde al codice
                     ✗ = divergenza tra UML e codice
                     ⚠ = nota / osservazione

================================================================================
Package Model
================================================================================

Classe NodeDTO: rappresenta un singolo nodo dell'albero.

	Attributi
	- leaf: boolean                                    ✓
	- splitCondition: String                           ✓
	- predictedValue: Double                           ✓
	- selected: boolean  (= true)                      ✓
	- children: List<NodeDTO>                          ⚠ UML usava "Children" (maiuscolo)
	                                                     Codice usa "children" (camelCase)

	Metodi
	+ NodeDTO(splitCondition: String)                  ✓
	+ NodeDTO(predictedValue: Double)                  ✓
	+ setLeaf(leaf: boolean)                           ✓
	+ isLeaf(): boolean                                ✓
	+ setSplitCondition(splitCondition: String)        ✓
	+ getSplitCondition(): String                      ✓
	+ setPredictedValue(predictedValue: Double)        ✓
	+ getPredictedValue(): Double                      ✓
	+ getChildren(): List<NodeDTO>                     ✓
	+ addChild(child: NodeDTO)                         ✓
	+ toString(): String                               ✓
	+ isSelected(): boolean                            ✓
	+ setSelected(selected: boolean)                   ✓

	Visibilità: Tutti gli attributi sono private (−), tutti i metodi public (+).
쳣_codice corretto.

	Relazioni:
	  - COMPOSIZIONE → NodeDTO (ricorsiva): un NodeDTO contiene una List<NodeDTO>
	    di figli (children). Il ciclo di vita dei figli è legato al padre.


Classe TreeDTO

	Attributi
	- root: NodeDTO                                    ✓
	- tableName: String                                ✓

	Metodi
	+ TreeDTO(root: NodeDTO, tableName: String)       ✓
	+ setRoot(root: NodeDTO)                           ✓
	+ getRoot(): NodeDTO                               ✓
	+ setTableName(tableName: String)                  ✓
	+ getTableName(): String                           ✓

	Visibilità: Tutti gli attributi sono private (−), tutti i metodi public (+).
异常_codice corretto.

	Relazioni:
	  - COMPOSIZIONE → NodeDTO: TreeDTO possiede il nodo radice (root) e ne
	    determina il ciclo di vita.


================================================================================
Package View
================================================================================

Classe TreePanel

	Attributi
	- currentTree: TreeDTO                             ✓ (duplicato rimosso nell'UML)
	- NODE_WIDTH: int = 150 statico {readonly}         ✗ UML: 140 — Codice: 150
	- NODE_HEIGHT: int = 44 statico {readonly}          ✗ UML: 45 — Codice: 44
	- VERTICAL_GAP: int = 70 statico {readonly}         ✓

	Metodi
	+ TreePanel()                                      ✓
	+ resetTree()                                      ✓
	+ updateTree(treeDTO: TreeDTO)                     ✓
	# paintComponent(g: Graphics)                      ✓  (manca @Override nell'UML)
	- drawTree(g2d: Graphics2D, node: NodeDTO,
	  x: int, y: int, availableWidth: int)             ✓
	- drawNodeBox(g2d: Graphics2D, node: NodeDTO,
	  x: int, y: int)                                  ✓
	- centerText(g2d: Graphics2D, text: String,
	  x: int, y: int, width: int, height: int)         ✓

	Estende JPanel                                     ✓

	Visibilità: private/public/protected corretti.
异常Nota: l'UML originale riportava "currentTree" duplicato (due volte).

	Relazioni:
	  - EREDITARIETÀ: estende JPanel (javax.swing)
	  - ASSOCIAZIONE → TreeDTO: mantiene riferimento a currentTree (uso)
	  - DIPENDENZA → NodeDTO: usata nei parametri di drawTree/drawNodeBox


Classe ControlPanel

	Attributi
	- ipField: JTextField                              ✓
	- portField: JTextField                            ✓
	- tableField: JTextField                           ✓
	- loadTreeButton: JButton                          ✓
	- sourceComboBox: JComboBox<String>                ✓
	- predictButton: JButton                           ✓

	Metodi
	+ ControlPanel()                                   ✓
	+ getServerAddress(): String                       ✓
	+ getServerPort(): int                             ✓
	+ getTableName(): String                           ✓
	+ getLoadTreeButton(): JButton                     ✓
	+ isDatabaseSource(): boolean                      ✓
	+ getPredictButton(): JButton                      ✓
	+ setPredictionEnabled(enabled: boolean)           ✓

	Estende JPanel                                     ✓

	Visibilità: Tutti gli attributi sono private (−), tutti i metodi public (+).
异常_codice corretto. Tutti gli attributi nel codice sono anche final.

	Relazioni:
	  - EREDITARIETÀ: estende JPanel (javax.swing)
	  - COMPOSIZIONE → JTextField (×3): ipField, portField, tableField
	  - COMPOSIZIONE → JButton (×2): loadTreeButton, predictButton
	  - COMPOSIZIONE → JComboBox<String>: sourceComboBox


Classe LogPanel

	Attributi
	- logArea: JTextArea                               ✓
	- scrollPane: JScrollPane                          ✓

	Metodi
	+ LogPanel()                                       ⚠ UML: "logPanel()" (minuscolo) —
	                                                     Codice: "LogPanel()" (corretto)
	+ log(message: String)                             ✓
	+ clear()                                          ⚠ UML: "clear" senza parentesi

	Estende JPanel                                     ✓

	Visibilità: Tutti gli attributi sono private (−), tutti i metodi public (+).
异常_codice corretto.

	Relazioni:
	  - EREDITARIETÀ: estende JPanel (javax.swing)
	  - COMPOSIZIONE → JTextArea: logArea
	  - COMPOSIZIONE → JScrollPane: scrollPane (contiene logArea)


Classe SummaryPanel

	Attributi
	- tableValueLabel: JLabel                          ✓
	- stepsValueLabel: JLabel                          ✓
	- predictionValueLabel: JLabel                     ✓

	Metodi
	+ SummaryPanel()                                   ✓
	- initMetaPanel(): JPanel                          ✓
	- initResultCard(): JPanel                         ✓
	+ setTable(tableName: String)                      ✓
	+ setSteps(steps: int)                             ✓
	+ setPrediction(value: Double)                     ✓  (UML: "Double", Codice: "Double")
	+ reset()                                          ✓

	Estende JPanel                                     ✓

	Visibilità: Tutti gli attributi sono private (−).
异常I metodi initMetaPanel/initResultCard sono private (−) come da UML.
异常Il costruttore e i setter sono public (+) come da UML.

	Relazioni:
	  - EREDITARIETÀ: estende JPanel (javax.swing)
	  - COMPOSIZIONE → JLabel (×3): tableValueLabel, stepsValueLabel,
	    predictionValueLabel


Classe MainFrame  (UML: "Mainframe")

	Attributi
	- controlPanel: ControlPanel                       ✓
	- treePanel: TreePanel                             ✓
	- summaryPanel: SummaryPanel                       ✓
	- logPanel: LogPanel                               ✓

	Metodi
	+ MainFrame()                                      ✗ UML: "Mainframe" — Codice: "MainFrame"
	+ getControlPanel(): ControlPanel                  ✓
	+ getTreePanel(): TreePanel                        ✓
	+ getSummaryPanel(): SummaryPanel                  ✓
	+ getLogPanel(): LogPanel                          ✓

	Estende JFrame                                     ✓

	Visibilità: Tutti gli attributi sono private final (−), tutti i metodi public (+).
异常_codice corretto. Nel codice gli attributi sono anche final.

	Relazioni:
	  - EREDITARIETÀ: estende JFrame (javax.swing)
	  - COMPOSIZIONE → ControlPanel: controlPanel
	  - COMPOSIZIONE → TreePanel: treePanel
	  - COMPOSIZIONE → SummaryPanel: summaryPanel
	  - COMPOSIZIONE → LogPanel: logPanel


================================================================================
Package Controller
================================================================================

Classe MainController

	Attributi
	- view: MainFrame                                  ✓
	- connection: ServerConnection                     ✓

	Metodi
	+ MainController(view: MainFrame)                  ✓
	- initListeners()                                  ✗ UML: private (−) — Codice: public (+)
	- handleInitTree()                                 ✓
	- handlePredict()                                  ✓

	Visibilità: Tutti gli attributi sono private (−).
异常DIVERGENZA: initListeners() è private (−) nell'UML ma public nel codice.
异常Suggerimento: dato che initListeners() viene chiamato solo dal costruttore,
异常la visibilità corretta dovrebbe essere private (−) come da UML.

	Relazioni:
	  - ASSOCIAZIONE → MainFrame: view (riferimento alla finestra principale)
	  - ASSOCIAZIONE → ServerConnection: connection
	  - DIPENDENZA → InitTreeWorker: creata in handleInitTree()
	  - DIPENDENZA → PredictionWorker: creata in handlePredict()
	  - DIPENDENZA → TreeDTO, NodeDTO: usate indirettamente


================================================================================
Package Network
================================================================================

Classe ServerConnection

	Attributi
	- socket: Socket                                   ✓
	- out: ObjectOutputStream                          ✓
	- in: ObjectInputStream                            ✓

	Metodi
	+ connect(ip: String, port: int)                   ✓
	+ isConnected(): boolean                           ✓
	+ send(obj: Object)                                ✓
	+ receive(): Object                                ✓
	+ close()                                          ✓

	Visibilità: Tutti gli attributi sono private (−), tutti i metodi public (+).
异常_codice corretto.

	Relazioni:
	  - COMPOSIZIONE → Socket: socket
	  - COMPOSIZIONE → ObjectOutputStream: out
	  - COMPOSIZIONE → ObjectInputStream: in
	  - Usa classi Java base: java.net.Socket,
	    java.io.ObjectOutputStream, java.io.ObjectInputStream


Classe InitTreeWorker

	Attributi
	- connection: ServerConnection                     ✓
	- ip: String                                       ✓
	- port: int                                        ✓
	- tableName: String                                ✓
	- fromDB: boolean                                  ✓
	- view: MainFrame                                  ✓

	Metodi
异常_costruttore pubblico con 6 parametri                ✗ UML: assente nel diagramma
	# doInBackground(): String                         ✓
	# done()                                           ✓

	Estende SwingWorker<String, Void>                  ✓

	Visibilità: Tutti gli attributi sono private (−).
异常I metodi overridden sono protected (#).

	Relazioni:
	  - EREDITARIETÀ: estende SwingWorker<String, Void> (javax.swing)
	  - ASSOCIAZIONE → ServerConnection: connection
	  - ASSOCIAZIONE → MainFrame: view
	  - DIPENDENZA → JOptionPane, JOptionPane: usati in done()


Classe PredictionWorker

	Attributi
	- connection: ServerConnection                     ✓
	- view: MainFrame                                  ✓
	- rootNode: NodeDTO                                ✓
	- currentNode: NodeDTO                             ✓

	Metodi
	+ PredictionWorker(connection: ServerConnection,
	  view: MainFrame)                                 ✓
	# doInBackground(): Double                         ✓
	- handleOkResponse(): Double                       ✓
	- handleQueryResponse(step: int): void             ✓
	- parseBranchOptions(queryText: String):
	  List<String>                                     ✓
	- promptUserForChoice(queryText: String): int      ✓
	# process(chunks: List<NodeDTO>): void             ✓
	# done(): void                                     ✓

	Estende SwingWorker<Double, NodeDTO>               ✓

	Visibilità: Tutti gli attributi sono private (−).
异常I metodi overridden sono protected (#), gli altri private (−) tranne il costruttore (+).

	Relazioni:
	  - EREDITARIETÀ: estende SwingWorker<Double, NodeDTO> (javax.swing)
	  - COMPOSIZIONE (inner class) → UserQueryDialog: classe interna
	  - ASSOCIAZIONE → ServerConnection: connection
	  - ASSOCIAZIONE → MainFrame: view
	  - DIPENDENZA → NodeDTO: usata per rootNode, currentNode
	  - DIPENDENZA → TreeDTO: creata in handleQueryResponse/process


	Inner class UserQueryDialog di PredictionWorker

	Attributi
	- queryText: String                                ✓
	- choice: int = -1                                 ✓

	Metodi
	+ UserQueryDialog(queryText: String)               ✓
	+ run()                                            ✓
	+ getChoice(): int                                 ✓

	Implementa Runnable                                 ✓

	Visibilità: Tutti gli attributi sono private (−), tutti i metodi public (+).
异常_codice corretto.

	Relazioni:
	  - Implementa: Runnable (interfaccia Java)
	  - Classe interna di: PredictionWorker (ha accesso ai campi esterni)
	  - DIPENDENZA → JOptionPane: usata per il dialogo utente


================================================================================
                          RIEPILOGO DIVERGENZE UML ↔ CODICE
================================================================================

1.  [✗] NodeDTO.children: UML usa "Children" (maiuscolo) — Codice usa "children"
2.  [✗] TreePanel.NODE_WIDTH: UML = 140 — Codice = 150
3.  [✗] TreePanel.NODE_HEIGHT: UML = 45 — Codice = 44
4.  [✗] MainFrame: UML scrive "Mainframe" — Codice usa "MainFrame"
5.  [✗] LogPanel: UML scrive costruttore "logPanel()" minuscolo — Codice: "LogPanel()"
6.  [✗] MainController.initListeners(): UML private (−) — Codice public (+)
7.  [✗] InitTreeWorker: manca il costruttore public nell'UML
8.  [⚠] LogPanel.clear: UML omette le parentesi "()"
9.  [⚠] TreePanel.paintComponent: UML omette l'annotazione @Override
10. [⚠] SummaryPanel.setPrediction: parametro "value: Double" — nel codice è "Double"
11. [⚠] TreePanel: l'UML originale elencava "currentTree" due volte (duplicato)

Nessuna divergenza funzionale critica trovata. Le visibilità sono corrette
tranne MainController.initListeners() che nel codice è public senza motivo.


================================================================================
                          RELAZIONI DI EREDITARIETÀ
================================================================================

TreePanel ──estende──▷ JPanel
ControlPanel ──estende──▷ JPanel
LogPanel ──estende──▷ JPanel
SummaryPanel ──estende──▷ JPanel
MainFrame ──estende──▷ JFrame
InitTreeWorker ──estende──▷ SwingWorker<String, Void>
PredictionWorker ──estende──▷ SwingWorker<Double, NodeDTO>
UserQueryDialog ──implementa──▷ Runnable


================================================================================
                    RELAZIONI DI AGGREGAZIONE / COMPOSIZIONE
================================================================================

── COMPOSIZIONE (il contenitore possiede e gestisce il ciclo di vita) ──

MainFrame          ◆── ControlPanel       (controlPanel)
MainFrame          ◆── TreePanel          (treePanel)
MainFrame          ◆── SummaryPanel       (summaryPanel)
MainFrame          ◆── LogPanel           (logPanel)
TreeDTO            ◆── NodeDTO            (root)
NodeDTO            ◆── NodeDTO            (children: List<NodeDTO> — ricorsiva)
LogPanel           ◆── JTextArea          (logArea)
LogPanel           ◆── JScrollPane        (scrollPane)
ServerConnection   ◆── Socket             (socket)
ServerConnection   ◆── ObjectOutputStream (out)
ServerConnection   ◆── ObjectInputStream  (in)
PredictionWorker   ◆── UserQueryDialog    (inner class)

── COMPOSIZIONE SWING (creati e gestiti internamente ai pannelli) ──

ControlPanel       ◆── JTextField         (ipField, portField, tableField)
ControlPanel       ◆── JButton            (loadTreeButton, predictButton)
ControlPanel       ◆── JComboBox<String>  (sourceComboBox)
SummaryPanel       ◆── JLabel             (tableValueLabel, stepsValueLabel,
                                           predictionValueLabel)

── ASSOCIAZIONE (riferimento, ciclo di vita indipendente) ──

TreePanel          ──→ TreeDTO            (currentTree: riferimento all'albero corrente)
MainController     ──→ MainFrame          (view: riferimento alla finestra)
MainController     ──→ ServerConnection   (connection: condivisa tra controller e workers)
InitTreeWorker     ──→ ServerConnection   (connection: condivisa)
InitTreeWorker     ──→ MainFrame          (view: per aggiornare la UI)
PredictionWorker   ──→ ServerConnection   (connection: condivisa)
PredictionWorker   ──→ MainFrame          (view: per aggiornare la UI)

── DIPENDENZA (uso temporaneo, creazione locale) ──

MainController     ──▷ InitTreeWorker     (creata in handleInitTree)
MainController     ──▷ PredictionWorker   (creata in handlePredict)
TreePanel          ──▷ NodeDTO            (usata nei metodi di disegno)
PredictionWorker   ──▷ TreeDTO            (creata in handleQueryResponse/process)
InitTreeWorker     ──▷ JOptionPane        (usata in done per dialoghi)
PredictionWorker   ──▷ JOptionPane        (usata in UserQueryDialog)

── DIPENDENZA DA CLASSI JAVA BASE / JAVASWING ──

JPanel             ◁── TreePanel, ControlPanel, LogPanel, SummaryPanel
JFrame             ◁── MainFrame
SwingWorker        ◁── InitTreeWorker, PredictionWorker
Socket             ◁── ServerConnection
ObjectOutputStream ◁── ServerConnection
ObjectInputStream  ◁── ServerConnection
JTextField         ◁── ControlPanel (×3)
JButton            ◁── ControlPanel (×2)
JComboBox          ◁── ControlPanel
JTextArea          ◁── LogPanel
JScrollPane        ◁── LogPanel
JLabel             ◁── SummaryPanel (×3)
JOptionPane        ◁── MainController, InitTreeWorker, PredictionWorker
JDialog            ◁── usata indirettamente via JOptionPane.showOptionDialog
Runnable           ◁── UserQueryDialog
Graphics / Graphics2D ◁── TreePanel (nei parametri di paintComponent)
ArrayList          ◁── NodeDTO (implementazione di children)
List               ◁── NodeDTO, PredictionWorker (interfaccia)
